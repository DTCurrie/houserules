---
'claude-kit': patch
---

doctor now warns when reference templates were committed before the kit ignored them, and update untracks them from git (keeping them on disk); template pointers note that npx claude-kit update restores the gitignored references.
