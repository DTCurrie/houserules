{
  "name": "@houserules/payload",
  "version": "0.2.0",
  "description": "houserules: shared payload libraries, copied into .claude/scripts/lib and executed on bare node with zero runtime dependencies.",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "payload-dist"
  ],
  "exports": {
    "./package.json": "./package.json",
    "./*": {
      "types": "./payload-dist/scripts/lib/*.d.mts",
      "default": "./payload-dist/scripts/lib/*.mjs"
    }
  },
  "scripts": {
    "build": "wireit",
    "check": "wireit",
    "test": "wireit",
    "prepack": "npm run build"
  },
  "wireit": {
    "build": {
      "command": "tsc -p tsconfig.payload.json && publint",
      "files": [
        "payload/**",
        "!payload/**/__tests__/**",
        "tsconfig.payload.json"
      ],
      "output": [
        "payload-dist/**"
      ]
    },
    "check": {
      "command": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.payload.json",
      "dependencies": [
        "../test:build"
      ],
      "files": [
        "payload/**/*.mts",
        "payload/**/__tests__/**/*.ts",
        "tsconfig.json",
        "tsconfig.payload.json"
      ],
      "output": []
    },
    "test": {
      "command": "vitest run",
      "dependencies": [
        "../test:build"
      ],
      "files": [
        "payload/**",
        "vitest.config.ts"
      ],
      "output": []
    }
  },
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DTCurrie/houserules.git",
    "directory": "packages/payload"
  },
  "homepage": "https://github.com/DTCurrie/houserules/tree/main/packages/payload#readme",
  "bugs": "https://github.com/DTCurrie/houserules/issues",
  "license": "MIT",
  "author": "Devin T. Currie (https://github.com/DTCurrie)",
  "keywords": [
    "houserules",
    "claude-code",
    "hooks"
  ],
  "devDependencies": {
    "@houserules/test": "workspace:*",
    "@types/node": "catalog:",
    "publint": "catalog:",
    "type-fest": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wireit": "catalog:"
  }
}
